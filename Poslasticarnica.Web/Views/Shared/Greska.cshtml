@model Poslasticarnica.Web.Models.GreskaViewModel

@{
    ViewData["Title"] = "Greška";
}

<h1>Greška</h1>

<h2>Došlo je do greške prilikom obrade zahteva.</h2>

@if (Model.PrikaziIdZahteva)
{
    <p>
        <strong>ID zahteva:</strong>
        <code>@Model.IdZahteva</code>
    </p>
}